<?php$id = $_POST['id'];include("../connections/conn.php");mysql_select_db($database_ndb, $ndb);$query = "insert into record (serial_num, format_code, country_code, label_name, prefix_to_number, issue_number, record_title, alternate_num, performers, director, solo, date_of_recording, comments) select serial_num, format_code, country_code, label_name, prefix_to_number, issue_number, record_title, alternate_num, performers, director, solo, date_of_recording, comments from record where id=".$id;$rs = mysql_query($query, $ndb) or die(mysql_error());?>