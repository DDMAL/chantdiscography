<?phpinclude_once('../classes/class.record.php');include_once('../classes/class.db.php');$record_id = $_POST['record_id'];$item_num = $_POST['item_num'];//$type_of_chant = $_POST['type_of_chant'];$track_num = $_POST['track_num'];$title_of_chant = $_POST['title_of_chant'];$page = $_POST['page'];$time = $_POST['time'];$comments = $_POST['comments'];//if ( $record_id == true) {$insertDB = new db;$insertDB->insertSQL = "INSERT INTO chant VALUES ('', '$record_id', '$item_num', '$track_num', '$title_of_chant', '$page', '$time', '$comments')";$insertDB->insert();$insertDB->selectSQL = "Select id, item_num, track_num, title_of_chant, page, time, comments from chant where record_id=".$record_id." ORDER BY item_num";$insertDB->select();/*$id = $insertDB->insertID;$newRecord = new record;$newRecord->fc = $fc;$newRecord->label = $label;$newRecord->insertID = $id; echo $newRecord->showTracks();echo $newRecord->showRecord();*///}?>